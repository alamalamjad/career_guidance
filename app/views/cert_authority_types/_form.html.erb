<%= form_for(@cert_authority_type) do |f| %>
  <% if @cert_authority_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cert_authority_type.errors.count, "error") %> prohibited this cert_authority_type from being saved:</h2>

      <ul>
      <% @cert_authority_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
